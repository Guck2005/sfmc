---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: sfmc
  labels: { app: user-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: user-service }
  template:
    metadata:
      labels: { app: user-service }
    spec:
      containers:
        - name: user-service
          image: sfmc/user-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3002
          env:
            - { name: PORT, value: "3002" }
            - { name: DB_HOST, value: "user-db" }
            - { name: DB_PORT, value: "5432" }
            - { name: DB_DATABASE, value: "sfmc_user" }
          envFrom:
            - configMapRef: { name: sfmc-config }
            - secretRef: { name: sfmc-secrets }
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          livenessProbe:
            httpGet: { path: /health, port: 3002 }
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /health, port: 3002 }
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: sfmc
spec:
  type: ClusterIP
  selector: { app: user-service }
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
