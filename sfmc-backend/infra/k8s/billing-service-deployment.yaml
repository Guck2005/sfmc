---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: sfmc
  labels: { app: billing-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: billing-service }
  template:
    metadata:
      labels: { app: billing-service }
    spec:
      containers:
        - name: billing-service
          image: sfmc/billing-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3007
          env:
            - { name: PORT, value: "3007" }
            - { name: DB_HOST, value: "billing-db" }
            - { name: DB_PORT, value: "5432" }
            - { name: DB_DATABASE, value: "sfmc_billing" }
          envFrom:
            - configMapRef: { name: sfmc-config }
            - secretRef: { name: sfmc-secrets }
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          livenessProbe:
            httpGet: { path: /health, port: 3007 }
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /health, port: 3007 }
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: sfmc
spec:
  type: ClusterIP
  selector: { app: billing-service }
  ports:
    - port: 3007
      targetPort: 3007
      protocol: TCP
