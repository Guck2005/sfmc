---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: reporting-service
  namespace: sfmc
  labels: { app: reporting-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: reporting-service }
  template:
    metadata:
      labels: { app: reporting-service }
    spec:
      containers:
        - name: reporting-service
          image: sfmc/reporting-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3009
          env:
            - { name: PORT, value: "3009" }
            - { name: DB_HOST, value: "reporting-db" }
            - { name: DB_PORT, value: "5432" }
            - { name: DB_DATABASE, value: "sfmc_reporting" }
          envFrom:
            - configMapRef: { name: sfmc-config }
            - secretRef: { name: sfmc-secrets }
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          livenessProbe:
            httpGet: { path: /health, port: 3009 }
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /health, port: 3009 }
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: reporting-service
  namespace: sfmc
spec:
  type: ClusterIP
  selector: { app: reporting-service }
  ports:
    - port: 3009
      targetPort: 3009
      protocol: TCP
