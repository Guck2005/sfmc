---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: production-service
  namespace: sfmc
  labels: { app: production-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: production-service }
  template:
    metadata:
      labels: { app: production-service }
    spec:
      containers:
        - name: production-service
          image: sfmc/production-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3006
          env:
            - { name: PORT, value: "3006" }
            - { name: DB_HOST, value: "production-db" }
            - { name: DB_PORT, value: "5432" }
            - { name: DB_DATABASE, value: "sfmc_production" }
          envFrom:
            - configMapRef: { name: sfmc-config }
            - secretRef: { name: sfmc-secrets }
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          livenessProbe:
            httpGet: { path: /health, port: 3006 }
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /health, port: 3006 }
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: production-service
  namespace: sfmc
spec:
  type: ClusterIP
  selector: { app: production-service }
  ports:
    - port: 3006
      targetPort: 3006
      protocol: TCP
